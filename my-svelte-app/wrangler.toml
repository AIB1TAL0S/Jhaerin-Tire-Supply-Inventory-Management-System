name = "jts-ims"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = ".svelte-kit/cloudflare"

[build]
command = "npm run build"

# Ensure wa-sqlite WASM assets are served correctly
[[rules]]
type = "Data"
globs = ["**/*.wasm"]
fallthrough = true
